import { ExpansionPanelHeaderTemplateDirective } from './expansion-panel-header-template.directive';

describe('ExpansionPanelHeaderTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpansionPanelHeaderTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
