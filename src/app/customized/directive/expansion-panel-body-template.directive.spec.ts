import { ExpansionPanelBodyTemplateDirective } from './expansion-panel-body-template.directive';

describe('ExpansionPanelBodyTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpansionPanelBodyTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
