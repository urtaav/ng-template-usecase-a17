import { DescriptionDirective } from './description.directive';

describe('DescriptionDirective', () => {
  it('should create an instance', () => {
    const directive = new DescriptionDirective();
    expect(directive).toBeTruthy();
  });
});
